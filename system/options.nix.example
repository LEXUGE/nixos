# Options of configuration

{ pkgs, ... }: {
  lib = {
    users = {
      ash = {
        enable = true;
        packages = with pkgs; [
          deluge
          hunspell
          hunspellDicts.en-us-large
          zoom-us
          thunderbird
          spotify
          firefox
          tdesktop
          emacs
          minecraft
          virtmanager
          # texlive.combined.scheme-full
        ];
      };
    };

    devices = {
      x1c7 = {
        enable = true;
        enableHowdy = false;
      };
    };

    system = {
      hostname = "nixos";
      proxy = {
        user = "ash"; # The user who would run the systemd service
        address = "127.0.0.1";
        localPort = 1080;
        redirPort = 1081;
      };
    };
  };
}
